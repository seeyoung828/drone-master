# System Architecture

## 1. 프로젝트 개요

본 프로젝트는 노지 환경에 분산된 ESP32-CAM 센서 노드로부터 이미지 데이터를 수집하기 위한 WiFi 기반 데이터 수집 시스템이다. 라즈베리파이 5는 이동형 마스터 노드 역할을 수행하며, 각 ESP32-CAM 센서 노드는 작물 이미지를 촬영하고 저장한 뒤 마스터 노드가 접근했을 때 데이터를 전송한다.

본 시스템은 고정형 AP 구축이 어려운 노지 환경을 고려하여, 드론 또는 이동형 장치가 마스터 노드 역할을 수행하는 구조를 목표로 한다.

## 2. 주요 구성 요소

| 구성 요소 | 역할 |
|---|---|
| Raspberry Pi 5 | 이동형 마스터 노드, 데이터 수집 및 세션 관리 |
| ESP32-CAM | 고정형 센서 노드, 작물 이미지 촬영 및 전송 |
| UDP Protocol | 이미지 chunk 전송 및 제어 메시지 교환 |
| SQLite DB | 센서 상태, 이미지 세션, 수신 chunk 상태 관리 |
| TFLite Module | 수집 이미지 기반 작물 병해 진단 연계 |

## 3. 전체 데이터 흐름

1. ESP32-CAM 센서 노드가 작물 이미지를 촬영한다.
2. 센서 노드는 전송 가능한 데이터가 있을 때 BEACON 메시지를 전송한다.
3. Raspberry Pi 마스터 노드는 센서 상태와 스케줄링 점수를 기반으로 GRANT를 부여한다.
4. 선택된 센서 노드는 이미지를 chunk 단위로 분할하여 DATA 메시지로 전송한다.
5. 마스터 노드는 수신 chunk를 파일에 저장하고 SQLite DB에 수신 상태를 기록한다.
6. 모든 chunk 수신 후 CRC32 검증을 수행하고 최종 이미지 파일로 확정한다.
7. 복원된 이미지는 TFLite 기반 진단 모듈과 연계될 수 있다.

## 4. 설계 목표

- 짧은 접촉 시간 내 이미지 데이터 수집
- 다중 센서 노드 간 수집 기회 분배
- UDP 기반 전송에서 발생할 수 있는 유실 보완
- chunk 단위 세션 복원
- 수집 이미지와 진단 결과의 연계 가능성 확보
