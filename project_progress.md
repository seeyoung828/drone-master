# 🚀 프로젝트 진행 상황 보고서 (Project Progress Report)

**프로젝트명:** WiFi 기반 노지 분산 IoT 센서 데이터 수집 기술 개발  
**기준 일자:** 2026년 4월 28일  
**현재 버전:** v2.6 (Intelligent Multi-Queue & Collision-Free Architecture)

---

## 1. 개요 (Overview)

본 프로젝트는 드론이 이동형 AP(Access Point)로 동작하며 노지에 분산된 센서들로부터 데이터를 공정하고 효율적으로 수집하는 시스템을 개발함. 최신 업데이트(v2.6)를 통해 **다중 이미지 큐(Multi-Image Queue)** 시스템을 완비하고, 다중 노드 환경에서의 **파일명 충돌 및 세션 전환 루프** 문제를 해결하여 시스템 안정성을 크게 향상시킴.

---

## 2. 주요 기능 구현 현황 (Implementation Status)

### ✅ 완료된 기능 (Done)

1.  **다중 이미지 큐(Multi-Image Queue) 구현**
    - **센서 노드:** `images/` 디렉토리 자동 스캔 및 전송 대기열(Queue) 관리.
    - **자동 전환:** 한 이미지의 수집·검증 완료 후 즉시 다음 이미지로 세션 자동 전환(Outer/Inner Loop 구조).
    - **파일명 기반 Data_ID:** 파일 수정 시간(mtime)을 활용해 고유한 `Data_ID`를 생성하여 중복 전송 방지.

2.  **다중 노드 세션 격리 및 충돌 방지**
    - **S_ID 접두어 도입:** 임시 파일(`.tmp`) 및 최종 파일(`.jpg`) 이름에 `s_id`를 추가(예: `S01_04281200.jpg`)하여 동일 `Data_ID` 사용 시의 파일 레이스 컨디션 해결.
    - **DB 쿼리 강화:** 모든 세션 관리 쿼리에 `s_id`와 `data_id`를 복합 키로 적용하여 데이터 정합성 보장.

3.  **에러 복구 및 프로토콜 안정화 (v2.6)**
    - **세션 전환 루프 해결:** 마스터의 유연한 세션 전환 허용 로직과 슬레이브의 `is_finished` 플래그 검증을 통해 `SESSION_MISMATCH` 무한 루프 차단.
    - **지능형 재시도:** `GRANT` 응답 없을 시 최대 3회 재시도 후 30초 패널티 부여로 드론 배터리 효율 최적화.
    - **가드 코드 추가:** `verify_and_finalize` 시 파일 존재 여부 및 조각 수(`received_count > 0`) 사전 체크로 크래시 방지.

4.  **프로토콜 문서 현행화 완료 (v2.6)**
    - `protocol_spec.md`를 v2.2에서 v2.6으로 업데이트하여 S_ID 기반 파일 구조 및 세션 전환 로직 명시 완료.

5.  **하이브리드 공정성 스케줄링 (Hybrid Fairness Scheduling)**
    - RSSI(30%), 데이터 잔여량(40%), Aging(30%)을 반영한 우선순위 스케줄링.
    - RSSI 강도에 따른 가변 전송량(N: 10~40) 동적 조정 구현.

### ⚠️ 부분 구현 / 수정 필요 (In Progress)

1.  **RSSI 실시간 연동:** `sensor_node.py`에서 여전히 고정값(-65) 사용 중. `iwconfig` 또는 `subprocess`를 통해 실제 무선 인터페이스에서 RSSI를 추출하는 로직 적용 필요.
2.  **명시적 전송 중단(Explicit Revoke) 프로토콜:** 현재는 마스터가 `GRANT`를 중단하는 방식이나, 센서가 즉시 대기 모드로 전환하고 다음 수집 기회를 준비할 수 있도록 하는 `STOP` 또는 `REVOKE` 명령 제어 로직 필요.

---

## 3. 향후 추진 계획 (Future Tasks)

### 🔹 단기 과제 (Next Steps)

- **강제 세션 종료 및 공정 분배 구현:** 드론 AP가 센서별 점유 시간을 모니터링하여 임계치 초과 시 **강제 종료 명령(Forced Termination)**을 전송하고, 즉시 다른 센서에게 권한을 부여하는 '선점형 스케줄링' 고도화.
- **순회 후 재접속(Resume) 메커니즘 최적화:** 중단 명령을 받은 센서가 드론이 다른 노드를 순회하고 돌아왔을 때, 이전 중단 지점(`last_idx + 1`)부터 충돌 없이 즉시 접속을 재개하는 상태 동기화 로직 보완.
- **C++ 포팅 (ESP32-CAM):** Python으로 검증된 v2.6 로직(큐잉, 에러 복구)을 ESP32 환경에 맞게 Arduino/C++ 코드로 이식.

### 🔹 중장기 과제 (Advanced Goals)

- **배터리 인식 가변 슬롯 정책:** 드론의 잔여 배터리에 따라 노드당 할당 시간($T$)을 동적으로 축소하여 전체 노드 수집 커버리지를 극대화하는 알고리즘 개발.
- **학술대회 성과 정리:** 라운드 로빈 및 고정 슬롯 방식 대비 제안하는 '지능형 강제 회수' 스케줄러의 수집 효율 비교 데이터 산출.

---

## 4. 기술적 이슈 및 해결 방안 (Issues & Solutions)

- **이슈 (전송 자원 독점):** 특정 노드의 신호가 양호할 때 전송이 길어져 다른 노드의 데이터가 유실되거나 수집 기회를 박탈당함.
- **해결 방안:** **명시적 회수(Revoke) 메커니즘**을 도입하여 슬롯 타임아웃 발생 시 마스터가 즉시 세션을 종료시키고, 해당 노드는 Aging 점수를 쌓으며 다음 순례 차례를 기다리도록 설계 변경 예정.

---
