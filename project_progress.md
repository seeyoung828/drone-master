# 🚀 프로젝트 진행 상황 보고서 (Project Progress Report)

**프로젝트명:** WiFi 기반 노지 분산 IoT 센서 데이터 수집 기술 개발  
**기준 일자:** 2026년 4월 9일  
**현재 버전:** v2.6 (Hybrid Fairness Scheduling & Stateful Protocol)

---

## 1. 개요 (Overview)

본 프로젝트는 드론이 이동형 AP(Access Point)로 동작하며 노지에 분산된 센서들로부터 데이터를 공정하고 효율적으로 수집하는 시스템을 개발하는 것을 목표로 함. 최신 업데이트(v2.2 정책 반영)를 통해 드론의 **배터리 보존(Energy-Efficient Collection)**과 **데이터 수집의 공정성(Fairness)**을 보장하는 **하이브리드 공정성 스케줄링** 및 **상태 기반(Stateful) 프로토콜**이 구현 완료됨.

---

## 2. 주요 기능 구현 현황 (Implementation Status)

### ✅ 완료된 기능 (Done)

1.  **상태 기반 UDP 비동기 프로토콜 (v2.2)**
    - 1024B 단위의 데이터 슬라이싱 및 바이너리/텍스트 하이브리드 헤더 구조.
    - `BEACON`, `GRANT`, `DATA`, `COMPLETE`, `ERROR` 메시지 시퀀스 구현.
    - **Pause & Resume:** 통신 중단 시 세션 상태를 DB에 저장하여 재접촉 시 `last_idx + 1`부터 즉시 재개 가능한 Stateful 구조 확립.

2.  **하이브리드 공정성 스케줄링 (Hybrid Fairness Scheduling)**
    - **Score 기반 타겟 선정:** RSSI(30%), 데이터 잔여량(40%), Aging(30%)을 반영한 우선순위 계산.
    - **전송 권한 회수 (Revoke):** **최대 점유 시간($T$)** 또는 **최대 처리 데이터량($N$)** 초과 시 즉시 권한을 회수하여 단일 노드의 독점 방지.
    - **동적 임계치 (Dynamic Thresholds):** RSSI 강도에 따라 한 슬롯당 전송량($N$)을 10~40개로 동적 조정하여 수집 효율 극대화.

3.  **데이터 무결성 및 연속성 관리 (State Management)**
    - **비트마스크(BLOB) 기반 세션 관리:** `received_mask`를 통해 수집된 조각을 추적하고 홀(Hole) 발생 시 우선 요청.
    - **Seek 기반 바이너리 기록:** 패킷 순서가 역전되더라도 파일 내 정확한 위치에 기록하는 무결성 보장 로직.
    - **CRC32 검증 및 원자적 확정:** 수집 완료 후 체크섬 대조를 통해 `.tmp`에서 `.jpg`로 변환.
    - **Purge 로직:** 동일 노드에서 새로운 데이터(`Data_ID`) 또는 전체 크기 변경 감지 시 기존 세션 자동 삭제.

4.  **에러 복구 및 패널티 시스템**
    - **스마트 재시도:** `GRANT` 송신 후 0.3초 내 응답 없을 시 최대 3회 재시도.
    - **패널티 시스템:** 재시도 횟수 초과(`TIMEOUT`) 시 해당 노드를 30초간 스케줄링에서 제외하여 드론 배터리 낭비 방지.
    - **자동 세션 복구:** CRC 불일치(`CHECKSUM_FAIL`) 발생 시 마스터가 슬레이브에게 알리고 DB 세션을 초기화하여 즉시 재수집 유도.

### ⚠️ 부분 구현 / 수정 필요 (In Progress)

1.  **RSSI 실시간 연동:** 현재 `sensor_node.py`에서 고정값(-65) 사용 중. 실제 무선 인터페이스(iwconfig 등)에서 추출하는 로직 필요. (라즈베리파이/노트북 환경 제약으로 인해 추후 통합 예정)
2.  **다중 이미지 큐 구현:** 센서 노드에 누적된 여러 장의 이미지를 순차적으로 전송하기 위한 큐잉 시스템 설계 중.

---

## 3. 향후 추진 계획 (Future Tasks)

### 🔹 단기 과제 (Next Steps)

- **C++ 포팅 (ESP32-CAM):** 현재 Python으로 구현된 `sensor_node.py` 로직을 ESP32 환경에 맞게 Arduino/C++ 코드로 변환 및 최적화.
- **Livelock 방지 검증:** 패킷 순서 역전 상황에서의 DB 무결성 및 Seek-Write 성능 스트레스 테스트.
- **다중 노드 시뮬레이션:** 3대 이상의 센서 노드 환경에서 공정성 스케줄링(Aging 가중치)의 실효성 분석.

### 🔹 중장기 과제 (Advanced Goals)

- **배터리 잔량 기반 동적 정책:** 드론의 배터리 잔량에 따라 $T$와 $N$의 가중치를 실시간 조정하는 알고리즘 고도화.
- **학술대회 논문 투고:** 라운드 로빈(Round Robin) 대비 제안하는 지능형 스케줄러의 수집 효율 및 공정성 성능 비교 논문 작성 (6월 한국통신학회).

---

## 4. 기술적 이슈 및 해결 방안 (Issues & Solutions)

- **이슈 (독점 방지):** 특정 노드가 신호가 좋아 대용량 데이터를 전송할 때 다른 노드의 수집 기회가 박탈됨.
- **해결:** **Revoke 메커니즘**을 도입하여 $T$(5초)와 $N$(가변) 임계치를 엄격히 적용하고, 전송이 중단된 노드는 Aging 점수를 통해 차후 수집 기회를 다시 보장받도록 설계함.

- **이슈 (배터리 효율):** 통신이 불량한 노드에 계속 `GRANT`를 시도할 경우 드론의 배터리가 소모됨.
- **해결:** **30초 패널티(Penalty) 시스템**을 적용하여 응답이 없는 노드는 대기 큐로 밀어내고, 신호가 양호한 다른 노드에게 즉시 권한을 부여하여 유효 수집 시간을 극대화함.

- **이슈 (데이터 무결성):** UDP 패킷 유실 및 순서 역전 시 파일이 깨질 위험이 있음.
- **해결:** DB에 **수신 마스크(received_mask)**를 도입하여 조각 단위로 추적하고, 파일 시스템의 **Seek-Write** 방식을 사용하여 인덱스 기반 정밀 기록을 수행함.

---
