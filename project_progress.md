# 🚀 프로젝트 진행 상황 보고서 (Project Progress Report)

**프로젝트명:** WiFi 기반 노지 분산 IoT 센서 데이터 수집 기술 개발  
**기준 일자:** 2026년 5월 4일  
**현재 버전:** v3.0 (Master-Side RSSI Tracking & High-Speed Transmission)

---

## 1. 개요 (Overview)

본 프로젝트는 드론이 이동형 AP(Access Point)로 동작하며 노지에 분산된 센서들로부터 데이터를 공정하고 효율적으로 수집하는 시스템을 개발함. 최신 업데이트(v3.0)를 통해 **Windows 환경의 노드 전송 병목을 완벽히 해결**하고, 마스터 주도형 RSSI 트래킹 아키텍처를 도입하여 시스템 전체의 신뢰성과 처리량을 극대화함.

---

## 2. 주요 기능 구현 현황 (Implementation Status)

### ✅ 완료된 기능 (Done)

1.  **마스터 주도형 RSSI 트래킹 (v3.0 New)**
    # ... (생략) ...

2.  **프로토콜 v3.0 경량화**
    # ... (생략) ...

3.  **데이터 수집 처리량 최적화 (v2.7/v3.0)**
    # ... (생략) ...

4.  **다중 이미지 큐(Multi-Image Queue) 및 세션 격리**
    # ... (생략) ...

5.  **하이브리드 공정성 스케줄링 (Hybrid Fairness Scheduling)**
    # ... (생략) ...

6.  **DB-물리 파일 정합성 강화 (v3.5 New)**
    - **이중 검증 메커니즘:** `prepare_session` 시 DB 상태와 실제 `.jpg` 파일 존재 여부를 동시 확인하여 유령 세션 방지.
    - **즉각적 수집 확정:** 이미 완료된 이미지에 대한 `BEACON` 수신 시 즉시 `COMPLETE_ACK`를 송신하여 불필요한 재수집 및 에러 발생 차단.
    - **검증 로직 멱등성:** `verify_and_finalize`에서 이미 완료된 파일에 대해 중복 요청이 와도 안전하게 성공 처리하도록 보완.


1.  **다중 노드 세션 정합성 강화 (v3.3 New)**
    - **고유 Data_ID 체계:** `S_ID`와 파일 메타데이터를 조합한 해시 기반 ID를 도입하여 다중 노드 동시 접속 시 세션 간섭을 원천 차단.
    - **Wait-for-Verdict (검증 대기):** 노드가 `COMPLETE` 송신 후 마스터의 판정을 기다리는 상태를 추가하여 제어 신호 동기화 불일치 해결.
    - **COMPLETE_ACK 도입:** 마스터의 최종 수집 확정 신호를 명시적으로 수신한 경우에만 파일을 완료 처리하도록 보완하여 데이터 신뢰성 확보.
    - **슬롯 카운트 정밀화:** 현재 할당된 타겟의 패킷만 통계에 반영하도록 로직 개선.

2.  **실환경 안정성 및 반응성 고도화 (v3.4 New)**
    - **Windows 소켓 오버플로우 해결:** `SO_SNDBUF` 확장 및 강화된 재시도 로직으로 `WinError 10035` 에러 완벽 차단.
    - **제어 신호 멱등성 확보:** 노드 측 슬롯 플래그 도입으로 중복 `REVOKE` 처리 오버헤드 및 로그 중첩 제거.
    - **스케줄링 반응성 최적화:** `GRANT` 재전송 주기(`0.8s`) 및 `IDLE_TIMEOUT`(`1.5s`) 미세 조정을 통해 네트워크 유실 대응력 강화.
    - **수집 간격 단축:** Verdict Waiting 타임아웃 단축 및 마스터의 `COMPLETE_ACK` 다중 송신으로 이미지 간 전환 속도 개선.

### ⚠️ 부분 구현 / 수정 필요 (In Progress)
*(현재 모든 v3.4 안정성 강화 로직이 구현 완료되었습니다.)*

---

## 3. 향후 추진 계획 (Future Tasks)

### 🔹 단기 과제 (Next Steps)

- **C++ 포팅 (ESP32-CAM):** Python으로 검증된 v3.0 로직(경량 프로토콜, 고속 전송)을 ESP32 환경에 맞게 Arduino/C++ 코드로 이식.
- **선점형 스케줄링 고도화:** 임계치 초과 시 강제 종료 명령을 전송하고, 즉시 다음 순위 노드로 전환하는 로직 안정화.
- **순회 후 재접속(Resume) 메커니즘 최적화:** 중단 명령을 받은 센서가 드론이 다른 노드를 순회하고 돌아왔을 때, 이전 중단 지점부터 충돌 없이 즉시 접속을 재개하는 상태 동기화 로직 보완.

### 🔹 중장기 과제 (Advanced Goals)

- **배터리 인식 가변 슬롯 정책:** 드론 배터리 잔량에 따른 노드당 할당 시간($T$) 동적 제어 알고리즘 개발.
- **비교 실험 데이터 산출:** 기존 라운드 로빈 및 고정 슬롯 방식 대비 v3.0 마스터 주도형 스케줄러의 수집 효율 정량적 비교.

---

## 4. 기술적 이슈 및 해결 방안 (Issues & Solutions)

- **이슈 (이미지 전송 속도 저하):** RSSI 실시간 연동 이후 Windows 노드에서 `netsh` 호출 지연(>0.5s)으로 인해 마스터의 `Idle Timeout`이 빈번하게 발생, 한 슬롯당 1개 미만의 청크만 수집됨.
- **해결 방안 (v3.0):** **전송 로직과 측정 로직을 분리**하여 RSSI 측정 책임을 리눅스 기반 마스터(Pi)로 이전함. 프로토콜에서 RSSI 필드를 제거하여 노드의 병목을 원천 차단하고 수집 속도를 정상화함.

- **이슈 (전송 자원 독점):** 특정 노드의 신호가 양호할 때 전송이 길어져 다른 노드의 데이터가 유실되거나 수집 기회를 박탈당함.
- **해결 방안:** **명시적 회수(Revoke) 메커니즘**을 도입하여 슬롯 타임아웃 발생 시 마스터가 즉시 세션을 종료시키고, 해당 노드는 Aging 점수를 쌓으며 다음 순례 차례를 기다리도록 설계 변경.

- **이슈 (간헐적 체크섬 오류 - CHECKSUM_FAIL):** 데이터 수집은 완료되었으나 CRC32 검증 단계에서 불일치가 발생하여 세션이 초기화됨.
- **해결 방안 (v3.1):** **원자적 검증(Atomic Verification)** 도입. 검증 직전 쓰기 핸들을 명시적으로 닫아(Flush & Close) OS 버퍼의 데이터를 디스크에 물리적으로 기록하고, 0.1초의 유예 시간을 두어 지연 도착한 UDP 패킷이 DB에 반영되도록 보완함.

- **이슈 (Data_ID 충돌 및 세션 간섭):** 동일한 이미지를 여러 노드에서 보낼 때 Data_ID가 충돌하여 마스터가 다른 노드의 수집 정보를 오적용(유령 완료 등)하거나, 이전 세션의 에러 신호가 다음 세션에 영향을 미치는 현상 발생.
- **해결 방안 (v3.3):** **고유 Data_ID 및 양방향 핸드셰이크** 도입. `S_ID`를 포함한 해시 기반 ID 생성으로 세션을 완벽히 격리하고, 노드가 `COMPLETE` 후 마스터의 `COMPLETE_ACK` 또는 `ERROR`를 기다리는 **Wait-for-Verdict** 상태를 추가하여 제어 신호의 선후 관계를 명확히 함.

- **이슈 (슬롯 통계 정확성):** 네트워크 상의 잔여 패킷이 현재 타겟의 슬롯 전송량(`chunks_in_slot`)으로 오인 집계되어 스케줄링 가중치가 왜곡됨.
- **해결 방안 (v3.3):** **타겟 기반 정밀 카운팅.** 패킷 수신부에서 `s_id == current_target` 인 경우에만 슬롯 통계에 합산하도록 로직을 교정하여 공정성 알고리즘의 신뢰도를 확보함.

- **이슈 (REVOKE 이후 이미지 스킵/Abort):** 마스터의 슬롯 종료(REVOKE) 명령을 노드가 "전송 폐기"로 오인하여 덜 보낸 이미지를 `sent/` 폴더로 치우고 다음 이미지로 넘어감.
- **해결 방안 (v3.2):** **상태 보존형(Stateful) 전송 재개.** `REVOKE`를 "슬롯 일시 반납"으로 재정의하고, **수집 성공 시에만 큐에서 제거(Pop-on-Success)**하도록 개선. 노드는 중단 지점을 기억했다가 다음 슬롯에서 즉시 Resume 수행.

- **이슈 (Windows 송신 버퍼 오버플로우 - WinError 10035):** 고속 전송 시 Windows의 기본 송신 버퍼 용량 초과로 인해 비차단 소켓 에러가 발생하며 슬롯이 강제 중단됨.
- **해결 방안 (v3.4):** **소켓 버퍼 확장 및 지능형 재시도.** `SO_SNDBUF`를 128KB로 확장하고, 에러 발생 시 대기 시간을 0.01s로 상향 조정한 10회 재시도 로직을 도입하여 전송 연속성을 확보함.

- **이슈 (제어 신호 중복 및 Verdict 지연):** 마스터의 다중 REVOKE 신호로 인한 로그 중첩과, 노드의 긴 Verdict 대기 시간(1.0s)으로 인해 수집 효율이 저하됨.
- **해결 방안 (v3.4):** **멱등성 플래그 및 타임아웃 최적화.** 슬롯 기반 멱등성 플래그를 도입하여 중복 신호를 차단하고, Verdict Waiting 타임아웃을 0.2s로 단축하여 이미지 간 전환 속도를 개선함.
