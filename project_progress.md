# 🚀 프로젝트 진행 상황 보고서 (Project Progress Report)

**프로젝트명:** WiFi 기반 노지 분산 IoT 센서 데이터 수집 기술 개발  
**기준 일자:** 2026년 4월 8일  
**현재 버전:** v2.5 (Error Handling & Recovery Integrated)

---

## 1. 개요 (Overview)

본 프로젝트는 드론이 이동형 AP(Access Point)로 동작하며 노지에 분산된 센서들로부터 데이터를 공정하고 효율적으로 수집하는 시스템을 개발하는 것을 목표로 함. 현재 핵심 통신 프로토콜 설계, 지능형 스케줄링, 그리고 예외 상황에 대비한 **에러 복구 로직**까지 구현이 완료된 단계임.

---

## 2. 주요 기능 구현 현황 (Implementation Status)

### ✅ 완료된 기능 (Done)

1.  **UDP 기반 비동기 프로토콜 (v2.1)**
    - 1024B 단위의 데이터 슬라이싱 및 바이너리/텍스트 하이브리드 헤더 구조 확립.
    - `BEACON`, `GRANT`, `DATA`, `COMPLETE` 메시지 시퀀스 구현.
2.  **지능형 스케줄링 알고리즘**
    - **Score 기반 타겟 선정:** RSSI(30%), 데이터 잔여량(40%), Aging(30%)을 반영한 우선순위 계산 로직.
    - **하이브리드 슬롯 제한:** 시간 제한(5s) 및 전송량 제한(N)을 통한 특정 노드 독점 방지.
    - **기회주의적 파라미터:** RSSI 세기에 따라 전송량(N)을 10~40개로 동적 조정.
3.  **데이터 무결성 및 연속성 관리**
    - **SQLite 기반 세션 관리:** `received_mask`(BLOB)를 이용해 수집된 조각을 비트 단위로 추적.
    - **원자적(Atomic) 파일 확정:** 수집 중에는 `.tmp` 유지, CRC32 검증 통과 시에만 `.jpg`로 변환.
    - **Purge 로직:** 동일 노드에서 새로운 데이터(`Data_ID`) 감지 시 기존 임시 파일 자동 삭제.
4.  **에러 패킷 처리 및 복구 로직 (v2.5) [NEW]**
    - **명시적 에러 피드백:** `TIMEOUT`, `CHECKSUM_FAIL`, `SESSION_MISMATCH` 에러 코드 정의 및 전송.
    - **스마트 재시도(Retry):** `GRANT` 후 응답 부재 시 0.3초 간격으로 최대 3회 재시도.
    - **패널티 시스템:** 통신 장애(`TIMEOUT`) 발생 노드에 대해 30초간 스케줄링 제외(Penalty).
    - **자동 세션 복구:** CRC 불일치 시 마스터가 슬레이브에게 에러를 알리고 DB 세션을 초기화하여 즉시 재수집 유도.

### ⚠️ 부분 구현 / 수정 필요 (In Progress)

1.  **RSSI 실시간 연동:** 현재 `sensor_node.py`에서 고정값(-65) 사용 중. 실제 무선 인터페이스(iwconfig 등)에서 추출하는 로직 필요.

- USB Wi-Fi 어댑터가 없어서 라즈베리파이에서 RSSI를 확인할 수 없고, 노트북에서 netsh wlan show interfaces를 활용하여 RSSI를 확인하는 방법 외에는 없음.
- 라즈베리파이가 특정 시점에 모든 RSSI를 검토하여 우선 순위를 결정해야 하기에, 해당 로직은 추후에 개발하기로 결정

2.  **Data_ID 규격화:** 8자리 문자열(MMDDHHmm) 생성을 위한 로직 반영 완료.

---

## 3. 향후 추진 계획 (Future Tasks)

### 🔹 단기 과제 (Next Steps)

- **다중 이미지 큐 구현:** 현재 1개의 `test.jpg`만 전송 가능. 센서 노드에 누적된 여러 장의 이미지를 순차적으로 전송하는 큐잉 시스템 추가.
- **C++ 포팅 (ESP32-CAM):** 수행계획서 상의 ESP32 환경에 맞게 `sensor_node.py` 로직을 C++/Arduino 코드로 변환 및 최적화.
- **Livelock 방지 강화:** 패킷 순서 역전 시에도 DB 무결성이 깨지지 않도록 하는 Seek-Write 로직 검증.
- **s:**

### 🔹 중장기 과제 (Advanced Goals)

- **드론 이동 경로 최적화 연동:** 수집된 RSSI 데이터를 기반으로 드론의 최적 체공 위치를 피드백하는 API 설계.
- **실제 노지 테스트:** 실제 WiFi 환경에서의 패킷 손실률 측정 및 재전송 효율(Implicit ACK) 분석.
- **학술대회 논문 투고:** 6월 한국통신학회 하계학술대회 발표를 위한 데이터 수집 성능 비교 분석 (Round Robin vs Intelligent Scheduler).

---

## 4. 기술적 이슈 및 해결 방안 (Issues & Solutions)

- **이슈:** UDP 특성상 패킷 유실 시 마지막 조각(`Last_Flag`)이 오지 않으면 무한 대기 발생 가능.
- **해결:** 드론 마스터에서 `SLOT_TIME_LIMIT`을 두어 강제 슬롯 종료 후, 다음 `GRANT` 시 유실된 첫 번째 인덱스(`get_next_missing_idx`)를 요청하여 홀(Hole)을 메우도록 구현함.

- **이슈:** 데이터 수집 완료 후 CRC32 체크섬 불일치 발생 시, 단순 타겟 해제만으로는 무한 반복 전송 위험이 있음.
- **해결:** `ERROR | CHECKSUM_FAIL` 패킷을 도입하여 슬레이브에게 명시적으로 알리고, 마스터의 DB 세션을 초기화(`reset_session`)하여 처음부터 다시 깨끗하게 수집을 시작하는 복구 시나리오를 적용함.

---
