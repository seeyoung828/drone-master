# 🚀 프로젝트 진행 상황 보고서 (Project Progress Report)

**프로젝트명:** WiFi 기반 노지 분산 IoT 센서 데이터 수집 기술 개발  
**기준 일자:** 2026년 5월 27일  
**현재 버전:** v3.9 (Handshake-based Active Monitoring & Scheduling Optimization)

---

## 1. 개요 (Overview)

본 프로젝트는 드론이 이동형 AP(Access Point)로 동작하며 노지에 분산된 센서들로부터 데이터를 공정하고 효율적으로 수집하는 시스템을 개발함. 최신 업데이트(v3.9)를 통해 **Handshake-based Active Monitoring** 기법을 도입하여, UDP 환경에서의 허위 연결 성공 문제를 해결하고 네트워크 단절 감지 능력을 극대화함.

---

## 2. 주요 기능 구현 현황 (Implementation Status)

### ✅ 완료된 기능 (Done)

1.  **Handshake-based Active Monitoring (v3.9 New)**
    *   **양방향 핸드셰이크 (IDLE_ACK):** 노드가 이미지가 없을 때 `"IDLE"` 비콘을 보내면 마스터가 즉시 `IDLE_ACK`를 반환하도록 개선. 노드는 이 응답을 확인해야만 "연결 정상"으로 판단하여 `sendto`의 허위 성공을 차단함.
    *   **반응형 대기 (Reactive Waiting):** 유휴 상태에서의 무조건적인 `time.sleep(5)`를 제거하고, 타임아웃(2.0s)이 설정된 `recvfrom` 기반 루프로 교체. 대기 중에도 마스터의 패킷을 즉시 수신할 수 있는 "청각 확보" 상태 유지.
    *   **IDLE 노드 스케줄링 즉시 배제:** 마스터가 `"IDLE"` 비콘 수신 시 해당 노드를 스케줄링 목록(`sensors_mem`)에서 즉시 삭제하고 `current_target`을 초기화하여 네트워크 효율성 향상.

2.  **순회 후 재접속(Resume) 및 가속 핸드셰이크 (v3.7)**
    - **가속 핸드셰이크:** 노드가 드론의 신호를 감지(Promiscuous Listening)하면 즉시 비콘 주기를 단축(0.1s)하여 재접속 반응성을 극대화.
    - **상태 영속성:** `node_state.json`을 도입하여 노드 프로그램 재시작 시에도 중단된 지점(`Data_ID`, `Index`)을 기억하고 즉시 Resume 수행.
    - **완료 가중치 스케줄링:** 수집률이 높은 노드에 우선순위를 부여. 점수 공식: `Score = (Aging*0.5) + (Completion*0.3) + (RSSI*0.2)`.

3.  **데이터 수집 처리량 및 프로토콜 최적화 (v3.0 - v3.6)**
    - **1KB 청크 사이즈 회귀 및 통일 (v4.0 Fix):** 성능 최적화를 위해 도입했던 4KB 설정을 폐기하고, UDP 전송에서의 IP 단편화(IP Fragmentation) 예방 및 ESP32-CAM 펌웨어 규격과의 정합성을 위해 **1KB(1024 bytes)**로 단위를 통일 및 확정.
    - **기회주의적 동적 N(Dynamic Thresholds):** RSSI에 따라 슬롯당 수집 청크 수를 동적으로 조정 (RSSI > -50: 40개, > -75: 20개, 이하: 10개).
    - **Early Exit 및 COMPLETE_ACK (v3.6):** DB상 완료된 세션은 물리 파일 유무와 관계없이 `COMPLETE_ACK`를 즉시 송신하여 불필요한 재수집 차단.
    - **Seek-Write 및 파일 핸들 캐싱:** DB 비트마스크(`received_mask`) 기반 Hole-filling 및 파일 핸들 유지로 디스크 I/O 병목 해결.

3.  **다중 노드 세션 정합성 및 안정성**
    - **결정적 Data_ID 생성:** `CRC32(S_ID + mtime + fsize)` 방식을 사용하여 노드 및 파일 간 세션을 완벽히 격리.
    - **Wait-for-Verdict (v3.4/v3.6):** 노드가 `COMPLETE` 후 마스터의 확정 신호를 기다리는 유예 시간을 0.1s~0.2s로 최적화하여 이미지 간 전환 속도 개선.
    - **Windows 소켓 안정화:** `SO_SNDBUF` 128KB 확장 및 `WinError 10035` 대응 10회 재시도 로직 도입.
    - **네트워크 장애 회복력:** 비콘 송신 실패(WinError 10065) 시 2초 대기 후 재시도하도록 구조를 개선하여 시스템 Hang 현상 방지.

4.  **선점형 스케줄링 안정화 및 Livelock 방지 (v3.8 New)**
    - **실시간 저신호 회수:** RSSI < -85dBm 감지 시 즉시 `REVOKE`를 수행하고 5초간 재스케줄링 제외 페널티 부여.
    - **Livelock 방지 (Master):** 동일 인덱스 연속 중복 수신(30회) 감지 시 `LIVELOCK_PREVENT` 에러 송신 및 세션 강제 중단.
    - **Livelock 대응 (Slave):** `LIVELOCK_PREVENT` 수신 시 인덱스 초기화, 상태 저장 및 2초간 백오프(Back-off) 실행으로 정체 해소.
    - **DB 저장 상태 세분화:** 신규/중복/에러 상태 반환을 통해 정밀한 세션 모니터링 구현.

5.  **루프 무한 대기 문제 해결 (v3.8)**
    - **IDLE 비콘 도입:** 전송할 이미지가 없는 유휴 상태에서도 `BEACON|IDLE`을 송신하여 네트워크 연결 상태를 실시간 감시.
    - **Active Monitoring:** 유휴 루프에서 5초마다 연결 상태를 확인하고, 단절 시 즉시 에러 로그 출력 및 재접속 대기 모드로 진입.
    - **마스터 연동:** 마스터가 IDLE 비콘을 수신하면 스케줄링 대상에서 제외하되 RSSI와 연결 상태(last_seen)만 갱신하도록 최적화.

6.  **스케줄링 공식 정밀화 및 가중치 최적화 완료 (v4.0 New)**
    *   **비선형 스케줄링 공식 도입:** 선형 스케줄링 공식에서 수학적 정밀도가 극대화된 비선형 공식($$Score = (W_1 \times \tanh(NormRSSI)) + \exp(W_2 \times NormCompletion) + (W_3 \times NormAging)$$)으로 전면 업그레이드 완료.
    *   **최적 가중치 실장:** 그리드 서치를 통해 입증된 비선형 파라미터 최적 가중치($W_1 = 0.26, W_2 = 0.48, W_3 = 0.26$)를 `drone_master.py` 실코드에 완벽하게 실장 및 검증.
    *   **시나리오 시뮬레이션 성공:** 오프라인 시뮬레이터(`test_nonlinear_scheduling.py`)를 통해 기아 상태 방지 및 완료 임박 세션의 초고속 마감(Exponential Boost) 실증 완료.

### ⚠️ 부분 구현 / 수정 필요 (In Progress)

- (현재 대기 중인 인프라/하드웨어 연동 및 C++ 포팅 관련 태스크들이 예정되어 있습니다)

---

## 3. 향후 추진 계획 (Future Tasks)

### 🔹 단기 과제 (Next Steps)

- **C++ 포팅 (ESP32-CAM):** Python으로 검증된 v3.7 로직(Resume, 가속 핸드셰이크)을 ESP32 C++ 코드로 이식.
- **안정성 필드 테스트:** 다양한 RSSI 환경에서의 선점형 회수 및 Livelock 방지 로직 정밀 검증.

### 🔹 중장기 과제 (Advanced Goals)

<!-- - **엣지 AI 진단 시스템 통합 (Edge AI Diagnosis):**
  - 라즈베리파이 5 내 TFLite 기반 MobileNetV2 모델 탑재 및 실시간 작물 병해 진단 파이프라인 구축.
  - 수집 완료된 이미지(`.jpg`)에 대한 자동 분석 및 진단 결과 DB 적재 기능 추가.
- **위치 기반 병해 관리 및 알림 시스템:**
  - 진단 결과가 임계값을 초과할 경우, 해당 노드의 위치 정보(좌표)와 함께 관리자에게 알림을 발송하는 기능 개발.
  - `sensors` 테이블 및 프로토콜에 위치 정보(GPS) 필드 추가 검토.
- **성능 정량 분석 및 비교 실험:**
  - 기존 라운드 로빈(Round Robin) 방식 대비 제안 알고리즘의 AoI(Age of Information) 개선율(약 19.38% 목표) 정밀 측정 및 데이터 산출. -->
- **배터리 인식 가변 슬롯 정책:** 드론 배터리 잔량에 따른 노드당 할당 시간($T$) 동적 제어 알고리즘 개발.

---

## 4. 기술적 이슈 및 해결 방안 (Issues & Solutions)

- **이슈 (이미지 전송 속도 저하):** RSSI 실시간 연동 이후 Windows 노드에서 `netsh` 호출 지연(>0.5s)으로 인해 마스터의 `Idle Timeout`이 빈번하게 발생, 한 슬롯당 1개 미만의 청크만 수집됨.
- **해결 방안 (v3.0):** **전송 로직과 측정 로직을 분리**하여 RSSI 측정 책임을 리눅스 기반 마스터(Pi)로 이전함. 프로토콜에서 RSSI 필드를 제거하여 노드의 병목을 원천 차단하고 수집 속도를 정상화함.

- **이슈 (전송 자원 독점):** 특정 노드의 신호가 양호할 때 전송이 길어져 다른 노드의 데이터가 유실되거나 수집 기회를 박탈당함.
- **해결 방안 (v3.1):** **명시적 회수(Revoke) 메커니즘**을 도입하여 슬롯 타임아웃 발생 시 마스터가 즉시 세션을 종료시키고, 해당 노드는 Aging 점수를 쌓으며 다음 순례 차례를 기다리도록 설계 변경.

- **이슈 (간헐적 체크섬 오류 - CHECKSUM_FAIL):** 데이터 수집은 완료되었으나 CRC32 검증 단계에서 불일치가 발생하여 세션이 초기화됨.
- **해결 방안 (v3.1):** **원자적 검증(Atomic Verification)** 도입. 검증 직전 쓰기 핸들을 명시적으로 닫아(Flush & Close) OS 버퍼의 데이터를 디스크에 물리적으로 기록하고, 0.1초의 유예 시간을 두어 지연 도착한 UDP 패킷이 DB에 반영되도록 보완함.

- **이슈 (REVOKE 이후 이미지 스킵/Abort):** 마스터의 슬롯 종료(REVOKE) 명령을 노드가 "전송 폐기"로 오인하여 덜 보낸 이미지를 `sent/` 폴더로 치우고 다음 이미지로 넘어감.
- **해결 방안 (v3.2):** **상태 보존형(Stateful) 전송 재개.** `REVOKE`를 "슬롯 일시 반납"으로 재정의하고, **수집 성공 시에만 큐에서 제거(Pop-on-Success)**하도록 개선. 노드는 중단 지점을 기억했다가 다음 슬롯에서 즉시 Resume 수행.

- **이슈 (Data_ID 충돌 및 세션 간섭):** 동일한 이미지를 여러 노드에서 보낼 때 Data_ID가 충돌하여 마스터가 다른 노드의 수집 정보를 오적용하거나, 이전 세션의 에러 신호가 다음 세션에 영향을 미치는 현상 발생.
- **해결 방안 (v3.3):** **고유 Data_ID 및 양방향 핸드셰이크** 도입. `S_ID`를 포함한 해시 기반 ID 생성으로 세션을 완벽히 격리하고, 노드가 `COMPLETE` 후 마스터의 `COMPLETE_ACK`를 기다리는 **Wait-for-Verdict** 상태를 추가하여 제어 신호 동기화 확보.

- **이슈 (슬롯 통계 정확성):** 네트워크 상의 잔여 패킷이 현재 타겟의 슬롯 전송량(`chunks_in_slot`)으로 오인 집계되어 스케줄링 가중치가 왜곡됨.
- **해결 방안 (v3.3):** **타겟 기반 정밀 카운팅.** 패킷 수신부에서 `s_id == current_target` 인 경우에만 슬롯 통계에 합산하도록 로직을 교정하여 공정성 알고리즘의 신뢰도를 확보함.

- **이슈 (Windows 송신 버퍼 오버플로우 - WinError 10035):** 고속 전송 시 Windows의 기본 송신 버퍼 용량 초과로 인해 비차단 소켓 에러가 발생하며 슬롯이 강제 중단됨.
- **해결 방안 (v3.4):** **소켓 버퍼 확장(128KB) 및 지능형 재시도.** `SO_SNDBUF` 확장 및 에러 발생 시 대기 시간을 상향 조정한 10회 재시도 로직을 도입하여 전송 연속성을 확보함.

- **이슈 (제어 신호 중복 및 Verdict 지연):** 마스터의 다중 `REVOKE` 신호로 인한 로그 중첩과, 노드의 긴 Verdict 대기 시간으로 인해 수집 효율이 저하됨.
- **해결 방안 (v3.4):** **멱등성 플래그 및 타임아웃 최적화.** 슬롯 기반 멱등성 플래그를 도입하여 중복 신호를 차단하고, Verdict Waiting 타임아웃을 0.2s로 단축하여 이미지 간 전환 속도 개선.

- **이슈 (재접속 지연):** 노드가 Back-off 상태(5s)일 때 드론이 재방문하면 수집 기회를 놓침.
- **해결 방안 (v3.7):** **가속 핸드셰이크(Promiscuous Listening)** 도입. 노드가 드론의 모든 패킷을 감청하여 신호 감지 즉시 비콘 주기를 0.1s로 초기화하도록 개선.

- **이슈 (문서 간 설정 불일치):** `scheduling_policy.md`(1KB)와 `protocol_spec.md`(4KB) 간의 청크 크기 혼선 발생.
- **해결 방안:** 실측 성능이 우수한 **4KB를 표준**으로 선정하였으나, 이후 실제 ESP32-CAM과의 연동 테스트 과정에서 UDP MTU 안전 규격(1500B 이하) 유지 및 IP 단편화 예방을 위해 최종적으로 **1KB(1024B)를 프로젝트 전체 공통 규격으로 통일** 및 최신화함.

- **이슈 (실제 ESP32-CAM 구동 시 지속적 CRC 검증 실패 - CHECKSUM_FAIL):** `sensor_node_test.cpp` 펌웨어를 실제 ESP32-CAM 모듈에서 실행 시, 11개 데이터 조각은 마스터에 100% 수집되나 무결성 검사 시 `CHECKSUM_FAIL`이 계속 발생하여 무한 재전송에 빠짐. 매 시도 시 로컬 CRC32와 원격 CRC32가 항상 동일한 오류 값(`0x580BDB73` vs `0x4F35CF7C`)을 보이는 결정론적 불일치 현상이 관측됨.
- **해결 방안 (v4.0 Fix):** 원인은 송신(C++: 1024B)과 수신(Python: 4096B) 간의 `CHUNK_SIZE` 비대칭이었음. 수신측이 `idx * 4096` 오프셋에 1024바이트씩 조립하면서 파일 중간중간에 3KB 크기의 Null(`\x00`) 패딩이 채워져 파일이 훼손(41,912 바이트로 비대해짐)되었던 것임. UDP 통신의 IP 단편화(IP Fragmentation) 방지 및 임베디드 펌웨어 사양과의 완벽한 정합성을 고려하여, **송수신 양측의 `CHUNK_SIZE` 상수를 `1024` (1KB)로 일치시키고 오프셋을 동기화**함. 아울러 이전 시도에서 손상된 세션 DB와 `.tmp` 파일을 원클릭으로 일괄 청소할 수 있는 [cleanup_session.py](file:///C:/Users/user/OneDrive/%EB%B0%94%ED%83%95%20%ED%99%94%EB%A9%B4/26.1%ED%95%99%EA%B8%B0/%EC%A2%85%ED%94%84/drone-master/server_udp/pi/cleanup_session.py) 스크립트를 배포하여 문제를 완벽하게 해결함.


