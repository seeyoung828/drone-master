# 🔍 루프 무한 대기 문제 분석 보고서 (Loop Error Analysis)

## 1. 문제 현상
드론(Raspberry Pi AP)과의 연결이 종료(Wi-Fi 해제 또는 AP 범위 이탈)되었음에도 불구하고, ESP 센서 노드에서 다음과 같은 로그가 반복적으로 출력되며 정상적인 재접속 시도나 에러 처리가 이루어지지 않음:
`--- [Waiting] 전송할 이미지가 없습니다. 5초 후 재검색... ---`

## 2. 원인 분석

### 2.1 잘못된 조건문 로직 (Main Loop Condition)
`sensor_node.py`의 `run()` 메서드 내 메인 루프 시작 부분의 조건문이 실제 네트워크 연결 상태나 이미지 큐의 상태를 정확히 반영하지 못하고 있습니다.

```python
# 문제의 코드 섹션
while True:
    self._scan_images()
    
    if not self.image_queue and not self.current_image_path:
        print("--- [Waiting] 전송할 이미지가 없습니다. 5초 후 재검색... ---")
        time.sleep(5)
        continue
```

- **상황:** 이미지를 모두 전송하여 `image_queue`가 비어 있고 `current_image_path`가 `None`인 상태에서 AP 연결이 끊기면, 위 `if` 문에 걸려 무한 대기 루프에 빠집니다.
- **결함:** 이 루프 안에서는 **네트워크 연결 상태를 확인하거나 재접속을 시도하는 로직이 전혀 없습니다.** 따라서 연결이 끊겨도 이미지가 없다는 이유만으로 계속 대기 로그만 출력하게 됩니다.

### 2.2 `_finalize_current_image`와 큐 관리의 허점
이미지 전송이 완료되어 `_finalize_current_image()`가 호출되면 `current_image_path`는 `None`이 되고 큐에서 해당 항목이 제거됩니다. 이때 다음 이미지가 없으면 센서는 즉시 "대기 모드"로 진입하는데, 이 대기 모드가 네트워크 상태와 독립적으로 설계되어 있어 발생한 문제입니다.

### 2.3 연결 감지 부재 (Connectivity Detection)
현재 노드는 `send_beacon()`을 호출할 때만 간접적으로 네트워크 오류를 감지(`socket.error`)할 수 있습니다. 하지만 이미지가 없을 때는 `send_beacon()`을 호출하는 `while True` (Inner Loop)에 진입조차 하지 않으므로 연결 끊김을 인지할 기회가 없습니다.

## 3. 해결 방안 (Proposed Solutions)

1. **대기 루프 내 네트워크 확인 로직 추가:** 이미지가 없는 대기 상태에서도 주기적으로 AP와의 연결 상태(RSSI 측정 또는 드론 IP 핑 등)를 확인해야 합니다.
2. **비콘 송신과 대기 로직의 통합:** 이미지가 없더라도 드론에게 "준비됨(IDLE)" 상태의 비콘을 주기적으로 보내 연결 유지를 확인하고, 드론이 다시 나타났을 때 즉시 반응할 수 있도록 구조를 개선해야 합니다.
3. **상태 머신(State Machine) 도입:** '연결됨/이미지 없음', '연결됨/전송 중', '연결 끊김/재접속 시도 중' 등의 상태를 명확히 분리하여 관리해야 합니다.

## 4. 결론
본 문제는 **이미지 큐가 비어 있는 상태에서의 예외 처리와 네트워크 연결 상태 감지 로직이 상호 분리되어 설계**되었기 때문에 발생한 것입니다. 이미지가 없더라도 네트워크 연결은 시스템의 핵심 전제 조건이므로, 대기 루프 내에서도 연결 상태를 모니터링하도록 수정이 필요합니다.
