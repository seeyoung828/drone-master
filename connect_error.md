현상 원인
해당 로그는 ESP32의 내부 Wi-Fi 드라이버(ESP-IDF)에서 출력하는 상태 충돌 에러 메시지입니다. Wi-Fi 스테이션(STA) 모듈이 이미 AP와 연결을 시도 중인(Connecting) 상태 머신에 머물러 있는데, 애플리케이션 계층에서 또 다시 연결 명령을 중복으로 인가했을 때 발생합니다.

시스템 상태 불일치와의 연관성
이 현상은 앞서 언급된 라즈베리파이(hostapd)의 고스트 세션 문제로 인해 파생된 결과입니다.

AP가 ESP32의 접속 요청을 비정상적인 접근으로 간주하여 응답하지 않으면, ESP32의 MAC 계층은 타임아웃이 발생할 때까지 인증 및 결합 대기 상태에 빠집니다.

작성하신 코드의 run 루프에서는 연결이 끊어졌다고 판단하면 WiFi.disconnect() 호출 후 1초 대기하고 즉시 WiFi.begin()을 재호출합니다.

WiFi.disconnect()는 비동기적으로 동작합니다. 내부 하드웨어 스택이 이전의 연결 시도를 완전히 취소(Abort)하고 상태를 초기화하기 전에 다음 WiFi.begin()이 실행되므로, 드라이버 레벨에서 상태 충돌(Already connecting) 에러를 반환하는 것입니다.

모델 검증 및 대안 제시
기존 코드는 연결이 끊어지면 단순 재시도 로직을 반복하는 조건에서는 성립하지만, AP 측의 인증 거부나 내부 상태 머신 꼬임이 발생한 상황에서는 상태 충돌 에러만 누적되며 시스템이 마비되므로 모델이 깨집니다.

단순히 WiFi.disconnect()를 호출하는 것이 아니라, 캐시를 지우고 내부 모뎀을 강제로 초기화하는 논리가 추가되어야 합니다.