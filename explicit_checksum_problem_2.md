# 🚨 명시적 전송 중단(REVOKE) 이후 이미지 스킵(Abort) 문제 분석 보고서 (v3.1)

## 1. 개요
v3.1 패치 이후 `REVOKE` 신호는 안정적으로 도달하고 있으나, 신호를 받은 노드가 전송 중이던 이미지를 **미완성 상태로 포기(Abort)하고 다음 이미지로 넘어가 버리는 현상**이 발생함. 이로 인해 대용량 이미지 수집이 불가능해지는 심각한 부작용이 관찰됨.

---

## 2. 문제 현상 분석 (`log_esp_test.txt` 기준)

- **현상:** `REVOKE` 수신 즉시 `--- [ABORT] Sensor_01 전송 중단 ---` 로그가 출력되며, 해당 이미지가 `sent/` 폴더로 이동됨.
- **결과:** 마스터는 "슬롯 종료(Chunk Limit)"로 인해 잠시 중단을 명령한 것이지만, 노드는 이를 "이미지 전송 실패 및 폐기"로 오인하여 다시는 해당 이미지를 보내지 않음.

---

## 3. 정확한 문제 원인 (Root Cause)

### 3.1 노드 제어 루프의 논리적 오류 (`sensor_node.py`)
노드의 `run()` 함수 구조가 "한 번 루프를 탈출하면 무조건 다음 이미지로 진행"하도록 설계되어 있음.

```python
# [문제의 코드 지점]
while True: # 메인 큐 루프
    self._prepare_next_image() # 큐에서 이미지를 꺼냄 (pop)
    while True: # 단일 이미지 전송 루프
        # ...
        elif msg[0] == "REVOKE":
            break # 루프를 탈출하여 위 while문으로 돌아감
    
    # 루프 탈출 후 로직
    if is_finished:
        self.send_complete()
    else:
        print("--- [ABORT] 전송 중단 ---")
    
    # [치명적 오류] 성공 여부와 상관없이 파일을 sent/ 폴더로 이동시키고 pop(0)된 상태 유지
    shutil.move(self.current_image_path, dest_path)
```

### 3.2 일시 중지(Pause)와 영구 중단(Abort)의 구분 부재
- **마스터의 의도:** "다른 노드에게 기회를 주기 위해 잠시 멈춰라. (Pause/Slot End)"
- **노드의 해석:** "이 이미지 전송은 끝났다. (Abort/Finish)"
- 현재 프로토콜 명세서(`protocol_spec.md`)에는 `REVOKE` 수신 시 "즉시 전송 루프를 탈출해야 한다"고만 되어 있어, 노드가 루프 탈출 후 **현재 이미지를 유지(Resume 준비)**할지 아니면 **버릴(Abort)**지에 대한 상세 정의가 부족함.

### 3.3 큐 관리 방식의 결함
- `_prepare_next_image`에서 `image_queue.pop(0)`을 수행하기 때문에, 전송에 실패하더라도 이미 큐에서 제거된 상태임. 재전송을 위한 복구 로직이 없음.

---

## 4. 파생 문제: 스케줄링 효율 저하
- 신호가 좋은 노드라도 대용량 이미지(300+ 청크)를 보낼 때, 마스터가 공정성을 위해 `max_n`(40개) 마다 `REVOKE`를 보내면, 노드는 40개만 보내고 해당 이미지를 버리게 됨.
- 결과적으로 모든 대용량 이미지가 깨진 상태로 수집되거나 수집이 누락됨.

---

## 5. 해결 방향 제안

1.  **노드 상태 머신 개선:** `REVOKE` 수신 시 `is_finished`가 `False`라면 파일을 `sent/`로 이동시키지 않고, 현재 `Data_ID`와 `current_idx`를 유지한 채 다시 `BEACON`을 보내는 상태로 복귀해야 함.
2.  **큐(Queue) 보존:** 이미지가 완전히 성공(`COMPLETE` 확인 또는 `is_finished` 확정)된 경우에만 큐에서 제거하고 `sent/`로 이동하도록 로직 변경.
3.  **마스터 재접속(Resume) 지원:** 마스터가 `REVOKE` 이후 다시 해당 노드에게 `GRANT`를 줄 때, 노드는 이전에 중단된 `current_idx`부터 전송을 재개할 수 있어야 함.
