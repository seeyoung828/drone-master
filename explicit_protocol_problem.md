# 🚨 명시적 전송 중단(Explicit Revoke) 프로토콜 부재 문제 분석

## 1. 개요
현재 시스템(v3.0)은 마스터가 스케줄링 점수에 따라 슬롯을 할당하고 종료하는 구조이나, 슬롯 종료 시 노드에게 이를 알리는 **명시적인 신호(Explicit Signaling)**가 부족함. 이로 인해 마스터가 슬롯을 종료했음에도 불구하고 노드는 여전히 잔여 데이터를 송신하여 네트워크 자원을 독점하는 문제가 발생함.

## 2. 세부 문제 원인 (Root Cause)

### 2.1 마스터의 단방향 슬롯 종료
- **코드 현황 (`drone_master.py`):**
    - `SLOT_TIME_LIMIT`(5초) 초과 또는 `max_n`(최대 40개) 도달 시, 마스터는 내부 변수인 `current_target`을 `None`으로 변경함.
    - 하지만, 해당 노드에게 전송을 멈추라는 명령(예: `REVOKE`)을 보내지 않음.
- **결과:** 마스터가 다른 노드로 타겟을 변경하더라도, 이전 노드는 마지막으로 `GRANT` 받은 개수(최대 40개)를 모두 보낼 때까지 채널을 계속 점유함.

### 2.2 노드의 중단 메커니즘 부재
- **코드 현황 (`sensor_node.py`):**
    - `send_data_chunks()` 함수는 요청받은 `count`만큼 루프를 돌며 데이터를 송신함.
    - 이 루프 중간에 마스터로부터 오는 패킷(중단 명령 등)을 확인하는 로직이 전혀 없음.
- **결과:** 한 번 전송을 시작하면 마스터의 의사와 상관없이 `GRANT` 받은 분량을 무조건 완수하려 함.

### 2.3 네트워크 자원 독점 및 충돌 (Monopoly & Collision)
- 마스터가 A 노드의 슬롯을 종료하고 B 노드에게 `GRANT`를 보냈을 때, A 노드가 여전히 전송 중이라면 두 노드의 패킷이 충돌함.
- 신호가 좋은 노드가 `SLOT_TIME_LIMIT`에 걸려 강제 종료되어도, 노드는 남은 전송을 계속하느라 다른 노드의 수집 기회를 방해함.

## 3. 요약 및 영향 분석
| 항목 | 내용 |
| :--- | :--- |
| **현상** | 슬롯 종료 후에도 이전 노드의 데이터 패킷이 지속적으로 수신됨 |
| **원인** | 마스터의 `REVOKE` 명령 부재 및 노드의 수신/중단 로직 부재 |
| **영향** | 스케줄링 공정성(Fairness) 훼손, 패킷 충돌로 인한 수신율 저하, 배터리 낭비 |

## 4. 해결 방향 제안

### [단계 1] 마스터: `REVOKE` 명령 추가
슬롯 종료 조건 만족 시, 해당 노드에 `REVOKE|[S_ID]|[Data_ID]` 메시지를 송신하여 즉시 중단을 요청함.

### [단계 2] 노드: 전송 루프 내 중단 체크
`send_data_chunks` 루프 내에서 소켓을 Non-blocking으로 체크하거나, 주기적으로(예: 5청크마다) 마스터의 중단 명령이 왔는지 확인하는 로직 추가.

### [단계 3] 프로토콜 정의 (`protocol_spec.md`)
`REVOKE` 또는 `STOP` 메시지 타입을 공식적으로 추가하여 전송 제어 계층을 강화함.
