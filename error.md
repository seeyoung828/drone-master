# ✅ (Resolved) 드론-센서 통합 테스트 중단 이슈 정밀 분석 보고서 (2026-05-20)

> **상태:** v3.6.1 패치 적용 완료 - 모든 고착 현상 및 무한 루프 해결됨. 상세 내용은 [solve_error.md](solve_error.md) 참조.

## 1. 장애 현상 요약 (Summary of Symptoms)

실환경 통합 테스트 중 센서 노드와 드론 마스터 간의 통신이 특정 지점에서 고착되어 더 이상 진행되지 않는 현상이 발생함.

- **드론 마스터 (`log_pi_test.txt`):**
    - `[Early Exit] Sensor_01 이미 완료된 세션입니다: 66eb837b` 메시지와 함께 `COMPLETE_ACK`를 무한히 송신함.
    - 새로운 데이터 수집 단계로 진입하지 못하고 동일한 노드에 대해 반복적으로 완료 확정 신호만 보냄.
- **센서 노드 (`log_esp_test.txt`):**
    - 초기 구동 시 `>>> Test1 timeout`이 수 회 발생한 뒤, `>>> Test1 error`가 무한히 반복 출력됨.
    - 이미지 전송을 시작(`GRANT` 수신)하지 못하고 대기 상태에 머무름.

---

## 2. 기술적 원인 분석 (Root Cause Analysis)

### 2.1 'Early Exit' 메커니즘과 프로토콜 불일치
본 장애의 근본 원인은 **v3.6에서 도입된 마스터의 'Early Exit' 최적화 로직을 센서 노드가 해석하지 못하는 프로토콜 정합성 결여**에 있음.

1.  **마스터의 동작 (v3.6 `drone_master.py`):**
    - 마스터는 노드로부터 `BEACON`을 수신하면 DB를 조회하여 해당 이미지(Data_ID)가 이미 완료된 상태인지 확인함.
    - 만약 완료된 상태라면, 불필요한 전송을 막기 위해 즉시 `COMPLETE_ACK` 패킷을 송신하고 해당 노드를 스케줄링 대상에서 제외함.
2.  **노드의 결함 (`sensor_node.py`):**
    - 노드의 메인 전송 루프(`run()` 메서드 내 `while True`)는 마스터로부터 `GRANT`, `REVOKE`, `ERROR` 메시지가 올 것만 예상하고 설계됨.
    - 마스터가 보낸 `COMPLETE_ACK`는 이 분기문에 해당하지 않으므로, 노드는 이를 '알 수 없는 메시지'로 간주하고 무시함.

### 2.2 로그 기반 상세 추적 (Trace)
로그와 소스 코드를 매칭한 결과, 다음과 같은 순서로 장애가 고착됨:

| 단계 | 주체 | 동작 및 로그 | 상세 분석 |
| :--- | :--- | :--- | :--- |
| **1** | 노드 | `BEACON` 송신 | `1.jpg (ID: 66eb837b)` 전송을 위해 마스터에 신호를 보냄. |
| **2** | 마스터 | `[Early Exit]` 판단 | DB 조회 결과 `66eb837b`는 이미 완료된 세션임. |
| **3** | 마스터 | `COMPLETE_ACK` 송신 | 노드에게 "이미 다 받았으니 전송하지 마라"는 의미로 ACK 송신. |
| **4** | 노드 | `recvfrom()` 성공 | 마스터가 보낸 `COMPLETE_ACK` 패킷을 수신함. |
| **5** | 노드 | **조건문 불일치** | `if msg[0] == "GRANT"` 등에서 모두 False 판정. |
| **6** | 노드 | `>>> Test1 error` 출력 | `try` 블록 하단의 디버그 구문에 도달하여 에러 로그 출력. |
| **7** | 노드 | 루프 재시작 | 다시 `BEACON`을 보내고 1단계로 회귀 (무한 반복). |

*참고: 초기 `>>> Test1 timeout`은 네트워크 일시 불안정 또는 마스터의 ARP 갱신 스레드로 인한 응답 지연으로 판단됨.*

---

## 3. 영향 평가 (Impact)

- **데이터 중복 수집 방지 오작동**: 마스터는 효율을 위해 전송을 거부하지만, 노드가 이를 이해하지 못해 지속적으로 비콘 쏘아 올림으로써 네트워크 대역폭을 낭비함.
- **이미지 큐 정체**: 노드가 현재 이미지의 완료 확정을 인식하지 못하므로, 다음 이미지(예: `2.jpg`)로 넘어가지 못하고 전체 시스템이 멈춤.

---

## 4. 최종 해결 계획 (Action Plan)

이 문제를 해결하기 위해 `solve_error.md`에 명시된 계획에 따라 **v3.6.1 패치**를 적용해야 함.

1.  **`sensor_node.py` 수정**:
    - 메인 전송 루프 내에 `msg[0] == "COMPLETE_ACK"` 처리 로직 추가.
    - 해당 메시지 수신 시 `is_finished = True`를 설정하여 즉시 루프를 탈출하고 다음 이미지로 전송 대상을 전환하도록 구현.
2.  **검증**:
    - 수정 후 마스터가 `COMPLETE_ACK`를 보낼 때 노드가 `[Early Exit] ...` 로그를 출력하며 즉시 다음 파일로 넘어가는지 확인.

---

<br>

# ✅ (Old/Resolved) 드론 수집 시스템 v3.5 실환경 테스트 오류 분석 보고서
*(v3.6 패치로 해결됨 - UDP 버퍼 관리 및 타임아웃 불일치 이슈)*
