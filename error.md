# 🚨 드론 수집 시스템 v3.5 실환경 테스트 오류 분석 보고서 (error.md)

## 1. 개요
v3.5 패치(DB-물리 파일 정합성 강화) 적용 후 다중 노드 통합 테스트를 수행함. `CHECKSUM_FAIL` 루프는 해결되었으나, 로그 분석 결과 **UDP 버퍼 관리 미흡**과 **타임아웃 설정 불일치**로 인한 비효율적인 재전송 및 스케줄링 간섭 현상이 새롭게 발견됨.

---

## 2. 주요 문제 현황 및 원인 분석

### 2.1 UDP 패킷 과소비 및 스케줄링 카운트 부정확 (Buffer Over-consumption)
- **현상:** 마스터 로그에서 `[Slot End]` 메시지가 매우 짧은 간격으로 여러 노드에 대해 교차 출력되며, 새로운 슬롯 시작 시 `[Retry]`가 빈번하게 발생함.
- **원인:** 마스터의 `recvfrom` 루프가 현재 타겟이 아닌 노드의 `DATA` 패킷까지 모두 소비(Consume)해버림. 
    1. 노드 A가 타겟일 때 노드 B가 보낸 잔여 패킷이 마스터 버퍼에 들어옴.
    2. 마스터는 이를 DB에 저장하지만, 현재 타겟(A)이 아니므로 `chunks_in_slot` 카운트에는 합산하지 않음.
    3. 이후 노드 B가 타겟이 되었을 때, 마스터는 이미 해당 패킷들을 소비했으므로 새로운 패킷을 기다리지만 노드는 이미 보냈다고 판단하여 대기함.
- **영향:** 불필요한 `GRANT` 재전송(0.8초 대기)이 발생하여 전체 수집 속도가 크게 저하됨.

### 2.2 노드-마스터 간 타임아웃 설정 불일치 (Timeout Mismatch)
- **현상:** 패킷 유실이 없더라도 간헐적으로 `[Retry]`가 발생하며, 노드는 정상적으로 완료 보고를 하지만 마스터는 재전송 후 수신함.
- **원인:** 노드의 소켓 타임아웃(`sock.settimeout(1.0)`)이 마스터의 재전송 주기(`0.8s`)보다 길게 설정되어 있음. 네트워크 지연 발생 시 노드가 아직 대기 중임에도 마스터가 먼저 타임아웃을 선언하고 재전송을 시도함.
- **영향:** 네트워크 자원 낭비 및 제어 신호 혼선 유발.

### 2.3 환경 불일치로 인한 잦은 세션 리셋 (Persistent DB Mismatch)
- **현상:** 테스트 시작 시 거의 모든 이미지 세션에 대해 `[Reset] ... 물리 파일이 없어 초기화합니다` 메시지가 출력됨.
- **원인:** 과거 테스트 이력으로 DB에는 `COMPLETED` 레코드가 존재하나, 실제 `collected_images/` 폴더는 비어있거나 파일이 이동된 상태임. v3.5 로직에 의해 정상적으로 복구는 되지만, 매번 전체 재수집이 발생함.
- **영향:** 수집 효율 저하. 이미 완료된 이미지라면 수집을 스킵하거나 노드에게 명시적으로 완료를 알리는 최적화 필요.

---

## 3. 향후 해결 방향 (v3.6 제안)

1.  **타겟 지향적 패킷 처리 (Target-Aware Processing):**
    - `recvfrom` 루프 내에서 현재 타겟이 아닌 노드의 `DATA` 패킷을 만날 경우, 이를 무조건 소비하지 않고 별도의 큐에 저장하거나 슬롯 카운트에 소급 적용하는 방안 검토.
2.  **타임아웃 파라미터 동기화:**
    - 노드의 소켓 타임아웃을 `0.5s` 이하로 단축하여 마스터의 재전송 로직보다 빠르게 반응하도록 수정.
3.  **세션 조기 종료 최적화 (Early Exit):**
    - `BEACON` 수신 시 DB 상태가 `COMPLETED`이면 물리 파일 유무와 관계없이 노드에게 `COMPLETE_ACK`를 우선 송신하여 불필요한 전송 루프 진입 방지.
4.  **스케줄링 통계 초기화 정밀화:**
    - 슬롯 전환 시 `chunks_in_slot` 뿐만 아니라 관련 타이머 및 카운터를 명시적으로 리셋하여 로그 노이즈 제거.
