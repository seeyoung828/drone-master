드론 마스터의 ESP 노드 스케줄링 및 선정 알고리즘

드론 마스터(drone_master.py)는 다수의 센서 노드(ESP)로부터 효율적으로 데이터를 수집하기 위해 멀티 팩터
스코어링(Multi-factor Scoring) 기반의 동적 스케줄링 알고리즘을 사용합니다.

---

1. 선정 핵심 지표 (Scoring Factors)

각 노드의 우선순위 점수(Score)는 다음 세 가지 요소를 결합하여 산출됩니다.

Score = (0.3 NormRSSI) + (0.4 NormRemaining) + (0.3 \* NormAging)

1.  신호 세기 (NormRSSI) - 비중 30%
    - 목적: 통신 품질이 좋은 노드에게 우선권을 부여하여 전송 성공률을 높이고 재전송을 최소화합니다.
    - 계산: max(0, (RSSI + 100) / 70)
    - -100dBm 이하를 0점으로, -30dBm 이상을 만점으로 정규화합니다.

2.  잔여 데이터량 (NormRemaining) - 비중 40%
    - 목적: 아직 전송해야 할 데이터가 많이 남은 노드를 우선적으로 처리하여 전체적인 작업 완료 속도를 맞춥니다.
    - 계산: (Total Chunks - Current Index) / Total Chunks
    - 전송 시작 전인 노드가 가장 높은 점수를 받습니다.

3.  대기 시간 (NormAging) - 비중 30%
    - 목적: 특정 노드가 기아 상태(Starvation)에 빠지는 것을 방지합니다. 신호가 약하더라도 오래 기다린 노드는 점수가  
      점진적으로 상승합니다.
    - 계산: min(1.0, WaitTime / 60.0)
    - 마지막으로 비콘이 확인된 후 60초가 지나면 만점에 도달합니다.

---

2. 제외 및 페널티 조건

점수 계산 전, 다음 조건에 해당하면 점수가 0점으로 처리되어 선정 대상에서 제외됩니다.

- Stale Timeout: 비콘이 10초(STALE_TIMEOUT) 동안 수신되지 않은 노드는 메모리에서 즉시 삭제됩니다.
- Retry Timeout: GRANT를 보냈으나 응답이 없어 재시도를 3회 초과한 경우, 30초간 선정 대상에서 제외됩니다.
- Cooldown Penalty: 방금 데이터 전송 슬롯이 종료된 노드는 2초간 쿨다운 기간을 가져, 다른 노드들에게 기회를 양보합니다.

---

3. 동적 슬롯 할당 (Dynamic N-limit)

노드가 선정된 후, 한 번에 요청할 청크 수(N)는 해당 노드의 RSSI에 따라 가변적으로 결정됩니다.

┌───────────────┬──────────────────┬──────────────────────────┐
│ RSSI 범위 │ 할당 청크 수 (N) │ 특징 │
├───────────────┼──────────────────┼──────────────────────────┤
│ -50 dBm 초과 │ 40 Chunks │ 근거리, 고속 대량 전송 │
│ -75 ~ -50 dBm │ 20 Chunks │ 중거리, 안정적 전송 │
│ -75 dBm 이하 │ 10 Chunks │ 원거리, 손실 최소화 전략 │
└───────────────┴──────────────────┴──────────────────────────┘

---

4. 노드 선정 및 실행 흐름

1. 비콘 수신: ESP 노드들이 주기적으로 보내는 비콘을 통해 sensors_mem 캐시를 업데이트합니다. 이때 DB를 조회하여 각 노드의
   실제 진행도(Missing Index)를 동기화합니다.
1. 스케줄러 작동: 현재 점유 중인 노드(current_target)가 없으면 스케줄링을 시작합니다.
1. 최고점 노드 탐색: 메모리에 있는 모든 노드의 점수를 calculate_score()로 계산하여 max_score를 가진 노드를 찾습니다.
1. 권한 부여 (GRANT): 선정된 노드에게 GRANT 패킷을 전송합니다. 이 패킷에는 수집을 시작할 인덱스와 동적으로 계산된  
   N_limit이 포함됩니다.
1. 슬롯 관리:
   - 지정된 청크 수를 모두 받았거나,
   - 최대 점유 시간(5.0초)이 경과하면 슬롯을 종료하고 다시 2번 과정으로 돌아갑니다.

---

5. 예외 상황 처리 (Robustness)

- CRC 검증 실패: 데이터 수집 완료 후 체크섬이 맞지 않으면 해당 세션을 초기화하고 타겟을 해제하여 다른 노드부터 재수집할  
  수 있도록 유도합니다.
- 세션 전환: 노드가 전송 중 갑자기 새로운 데이터(다른 data_id)를 보내면 이를 감지하여 유연하게 세션을 교체합니다.
