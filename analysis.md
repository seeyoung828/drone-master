# 🛸 드론 기반 데이터 수집 시스템 프로젝트 분석 보고서 (analysis.md)

본 문서는 `drone-master` 프로젝트의 전반적인 아키텍처, 코드 구조, 프로토콜 및 현재 구현 현황을 분석한 결과입니다.

---

## 1. 프로젝트 개요
- **목적:** 드론(Raspberry Pi)을 이동형 AP로 활용하여 광범위하게 분산된 센서 노드(ESP32-CAM)로부터 이미지 및 센서 데이터를 효율적으로 수집.
- **주요 특징:** 
    - UDP 기반 상태 유지(Stateful) 비동기 통신.
    - 하이브리드 공정성 스케줄링(RSSI, 잔여 데이터량, 대기 시간 고려).
    - 중단된 시점부터 이어서 받는 Resume 메커니즘.
    - 체크섬(CRC32)을 통한 데이터 무결성 보장.

---

## 2. 시스템 아키텍처

### [Master-Slave 구조]
```text
[ Sensor Node (Slave) ] <---- UDP (5005) ----> [ Drone (Master) ]
      (ESP32-CAM)                                (Raspberry Pi 4)
           |                                            |
    - 이미지 큐 관리                                - 스케줄링 (Score 기반)
    - 4KB 청크 단위 전송                            - RSSI 실시간 트래킹
    - Resume 지원                                  - SQLite 기반 수집 관리
    - 완료 보고 (CRC32)                             - 데이터 무결성 검증
```

---

## 3. 코드 구조 분석

### 3.1 디렉토리 구조
- `/docs`: 프로토콜 및 스케줄링 정책 명세서.
- `/server_udp/pi`: 마스터(드론) 측 구현 코드 (Python, SQLite).
- `/server_udp/esp` & `/esp1`: 슬레이브(센서 노드) 시뮬레이션/테스트 코드.
- `/server_udp/common`: 공통 상수 및 파싱 유틸리티.
- `/logs`: 테스트 로그 파일들.
- `/task_problem` & `*.md`: 문제 발생 시점의 에러 로그 및 해결 과정 기록.

### 3.2 핵심 소스 파일
| 파일명 | 역할 | 주요 기능 |
| :--- | :--- | :--- |
| `drone_master.py` | 시스템 컨트롤러 | 노드 스케줄링, GRANT/REVOKE 제어, RSSI 트래킹, 패킷 처리 루프 |
| `db_manager.py` | 데이터 관리 | SQLite 연동, 비트마스크 기반 수집 진행도 관리, 파일 최종화(CRC32 검증) |
| `sensor_node.py` | 노드 에뮬레이터 | 이미지 스캔, BEACON 송신, 데이터 청크 전송, Resume 로직 |
| `utility.py` | 공통 모듈 | 4KB CHUNK_SIZE 정의, 패킷 헤더/페이로드 파싱 |

---

## 4. 통신 프로토콜 (v3.4)

### 4.1 패킷 헤더 구조
`[Type]|[S_ID]|[Data_ID]|[Total_Chunks]|[Current_Idx]|[Last_Flag]|[Payload]`

- **Type:** BEACON, GRANT, DATA, COMPLETE, COMPLETE_ACK, REVOKE, ERROR
- **Data_ID:** 이미지 파일의 고유 식별자 (S_ID + 수정시간 + 크기 기반 해시).

### 4.2 주요 통신 시퀀스
1. **발견:** 노드가 `BEACON`을 주기적으로 송신.
2. **권한 부여:** 마스터가 스케줄링 점수가 가장 높은 노드에게 `GRANT` 송신 (시작 인덱스 및 전송량 N 지정).
3. **데이터 전송:** 노드가 지정된 양만큼 `DATA` 청크 전송.
4. **강제 중단(선택):** 슬롯 시간 초과 시 마스터가 `REVOKE` 송신 -> 노드는 즉시 중단 후 비콘 상태로 복귀.
5. **완료:** 노드가 모든 청크 전송 후 `COMPLETE` (CRC32 포함) 송신.
6. **확정:** 마스터가 검증 후 `COMPLETE_ACK` 송신 -> 노드는 파일을 `sent/` 폴더로 이동.

---

## 5. 핵심 알고리즘 및 기술

### 5.1 하이브리드 공정성 스케줄링
다음 점수 공식을 통해 전송 우선순위를 결정:
- `Score = (0.3 * NormRSSI) + (0.4 * NormRemaining) + (0.3 * NormAging)`
- RSSI가 좋을수록, 데이터가 많이 남았을수록, 오랫동안 못 보냈을수록 가중치 부여.

### 5.2 성능 최적화 (v3.0+)
- **Master-Side RSSI Tracking:** 노드에서 RSSI를 측정하지 않고 마스터가 `iw station dump`로 직접 측정하여 노드 부하 감소.
- **SQLite WAL 모드:** DB I/O 병목 해결.
- **4KB 청크 사이즈:** UDP 패킷 효율 최적화.
- **Windows 소켓 버퍼 확장:** 고속 전송 시 `WinError 10035` 방지를 위해 `SO_SNDBUF`를 128KB로 상향.

---

## 6. 현재 상태 및 향후 과제

### 현재 상태
- **안정성:** v3.4 버전에서 소켓 버퍼 이슈, 세션 정합성, 체크섬 에러 등이 대부분 해결됨.
- **기능:** 다중 이미지 연속 전송 및 중단 후 재개(Resume) 기능이 안정적으로 동작.

### 향후 과제
1. **C++ 포팅:** 현재 Python으로 구현된 노드 로직을 ESP32-CAM 환경(Arduino/C++)으로 이식 필요.
2. **배터리 관리:** 드론의 배터리 잔량에 따른 동적 슬롯 시간($T$) 조정 로직 추가.
3. **실환경 테스트:** 다중 노드(3개 이상) 환경에서의 실시간 스케줄링 효율성 검증.

---
**작성일:** 2026년 5월 5일  
**작성자:** Gemini CLI 분석 도구
