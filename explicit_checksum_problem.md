# 🚨 명시적 전송 중단(REVOKE) 및 체크섬(CHECKSUM_FAIL) 문제 분석 보고서

## 1. 개요
v3.0 프로토콜 도입 및 `REVOKE` 메커니즘 추가 이후, 테스트 결과(`test_result.txt`)에서 **REVOKE 신호 무시** 현상과 간헐적인 **CHECKSUM_FAIL** 오류가 관찰됨. 본 문서는 해당 문제들의 상세 원인을 분석함.

---

## 2. REVOKE 신호 전달 및 처리 실패 원인

### 2.1 메인 루프에서의 패킷 소비 (Main Loop Consumption)
- **증상:** 마스터가 `REVOKE`를 보내도 노드가 멈추지 않고 `GRANT` 받은 분량을 모두 전송함.
- **원인:** `sensor_node.py`의 메인 `while True` 루프는 `self.sock.recvfrom(2048)`을 통해 모든 수신 패킷을 가져옴. 하지만 해당 루프의 조건문(`if/elif`)에는 `GRANT`와 `ERROR`만 처리하고 있어, **`REVOKE` 패킷이 수신되더라도 아무런 동작 없이 버려짐.**
- **결과:** 노드가 `send_data_chunks` 루프 외부에 있을 때 도착한 `REVOKE`는 증발하며, 루프 진입 직전이나 비콘 대기 상태에서 전송 중단 명령을 인지할 수 없음.

### 2.2 단발성 전송 및 UDP 유실 (Single Transmission & UDP Loss)
- **원인:** 마스터는 슬롯 종료 시 `REVOKE`를 단 한 번만 송신함. UDP 프로토콜의 특성상 유실 가능성이 있으며, 특히 노드가 고속으로 `DATA` 패킷을 쏟아내고 있는 상황에서는 네트워크 버퍼 오버플로우로 인해 마스터의 제어 패킷이 유실될 확률이 높아짐.

---

## 3. 간헐적 CHECKSUM_FAIL 발생 원인

### 3.1 파일 핸들 캐싱 및 플러시 지연 (Unflushed File Buffer) - **주요 원인**
- **원인:** `drone_master.py`는 성능 최적화를 위해 `db.save_fragment`에서 파일 핸들을 캐싱하여 열어둠. 이 핸들은 슬롯이 완전히 종료될 때(`db.close_file()`)만 닫힘.
- **문제점:** 노드가 전송을 마치고 즉시 `COMPLETE`를 보내면, 마스터는 `verify_and_finalize`를 호출하여 파일을 다시 `rb` 모드로 열어 CRC32를 계산함. 이때 **기존 쓰기 핸들에 남아있는 데이터가 디스크에 플러시(Flush)되지 않은 상태**라면, 읽기 루틴은 불완전한 데이터를 읽게 되어 CRC 값이 불일치하게 됨.
- **결과:** 데이터는 정상적으로 도착했으나, 검증 시점에 OS/애플리케이션 버퍼에 데이터가 머물러 있어 체크섬 오류가 발생함.

### 3.2 Data_ID 충돌 (Data_ID Collision)
- **원인:** 현재 `Data_ID`는 파일의 수정 시간(`mtime`)을 초 단위(`%d%H%M%S`)로 파싱하여 생성함.
- **문제점:** 여러 장의 이미지를 한꺼번에 복사하거나 생성하여 `mtime`의 초 단위가 동일할 경우, 서로 다른 이미지임에도 같은 `Data_ID`를 가지게 됨.
- **결과:** 마스터 입장에서 이전 세션의 잔여 데이터(마스크, 임시 파일)가 남아있는 상태에서 동일 ID의 신규 세션이 시작되면, 기존 조각을 중복으로 간주하거나 잘못된 위치에 덮어씌워 최종 체크섬이 깨짐.

### 3.3 UDP 패킷 순서 역전 및 지연 도착 (Late Data Arrival)
- **원인:** 노드는 마지막 `DATA` 패킷 송신 직후 `COMPLETE`를 보냄. 
- **문제점:** 네트워크 지연으로 인해 `COMPLETE`가 마지막 `DATA` 조각들보다 먼저 도착하거나, 마스터의 처리 큐에서 `COMPLETE`가 먼저 처리될 경우 `received_count < total_chunks` 상태에서 검증이 시도되어 실패함.

---

## 4. 해결 방향 제안

1.  **Node 수정:** 메인 루프에서도 `REVOKE`를 처리하도록 분기 추가 및 세션 초기화 로직 연동.
2.  **Master 수정:** `verify_and_finalize` 호출 직전 반드시 `db.close_file()`을 수행하여 버퍼를 비우고 핸들을 정리하도록 보완.
3.  **Data_ID 개선:** 초 단위 시간 외에 파일 크기나 랜덤 값을 추가하여 ID 충돌 가능성 제거.
4.  **검증 재시도:** `COMPLETE` 수신 시 즉시 실패 처리하기보다, 아주 짧은 대기(예: 0.1s) 후 최종 조각 도착 여부를 재확인하는 유연한 검증 도입.
